# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                     controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file         controllers.Assets.at(path="/public", file)

# New map page
GET        /new                  controllers.Application.newMap

# Specific (read or write) server-stored map
GET        /maps/:id             controllers.Application.getMap(id)

# Save latest map data
GET        /maps/:id/save        controllers.Application.saveMap(id, content)

# Load map data
GET        /maps/:id/load        controllers.Application.loadMap(id)
