# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                        controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file            controllers.Assets.at(path="/public", file)

# New map page
GET        /new                     controllers.Application.newMap

# Specific (read or write) server-stored map
GET        /maps/:mapId             controllers.Application.getMap(mapId)

# Save latest map data
GET        /maps/:mapId/save        controllers.Application.saveMap(mapId, cmdId, content)

# Load map data
GET        /maps/:mapId/load        controllers.Application.loadMap(mapId)

